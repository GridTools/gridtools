if( NOT @UPPERCASE_PROJECT_NAME@_TARGETS_EXPORTED ) 
  include( ${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@-exports.cmake )
endif()

set( GRIDTOOLS_HAVE_CUDA @GRIDTOOLS_HAVE_CUDA@ )
if( GRIDTOOLS_HAVE_CUDA )

  set( CUDA_NVCC_FLAGS ${CUDA_NVCC_FLAGS} @GRIDTOOLS_NVCC_FLAGS@ )
  set( CUDA_USE_STATIC_CUDA_RUNTIME OFF )
  set( CUDA_TOOLKIT_ROOT_DIR "@CUDA_TOOLKIT_ROOT_DIR@" )
  find_package(CUDA REQUIRED)
  set( CUDA_PROPAGATE_HOST_FLAGS ON )
  set( CUDA_SEPARABLE_COMPILATION ON )

endif()
